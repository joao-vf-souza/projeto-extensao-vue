# Workflow para build e deploy no GitHub Pages
name: Deploy to GitHub Pages

on:
  # Executa em push para a branch main
  push:
    branches: [ main ]
  
  # Permite execução manual do workflow
  workflow_dispatch:

# Define permissões para o token GITHUB_TOKEN
permissions:
  contents: read
  pages: write
  id-token: write

# Permite apenas um deploy simultâneo, cancelando execuções em progresso
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Job de build
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: './projeto-extensao-vue/package-lock.json'
          
      - name: Install dependencies
        run: |
          cd projeto-extensao-vue
          npm ci
          
      - name: Build
        run: |
          cd projeto-extensao-vue
          npm run build
          
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './projeto-extensao-vue/dist'

  # Job de deploy
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4